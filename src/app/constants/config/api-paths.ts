import { environment } from "src/environments/environment";

/**
 * Contains the different API paths for the mock API
 */
export const API_PATHS = {
  products: `${environment.mockApiHost}/api/v1/products/get-all`
};